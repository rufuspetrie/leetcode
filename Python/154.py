# Initial thoughts
    # This is basically like the previous question but because there can be ties for
        # the endpoints, you can't tell whether an array is rotated from one comparison
    # Basically, the person who wrote this question is a brainlet
class Solution:
    def findMin(self, nums: List[int]) -> int:
        return min(nums)